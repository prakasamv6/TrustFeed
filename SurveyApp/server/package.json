{
  "name": "trustfeed-survey-api",
  "version": "1.0.0",
  "description": "TrustFeed Survey REST API — stores anonymous survey data in MySQL (NO PII)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node --test",
    "smoke": "node scripts/smoke-check.js",
    "smoke:json": "node scripts/smoke-check.js --json"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "helmet": "^7.2.0",
    "mysql2": "^3.9.1"
  }
}
